# 📚 SKOLKOVO Materials Processor

Система для обработки учебных материалов через Claude API:
- **PDF документы** — извлечение и анализ текста
- **Аудио записи** — транскрипция семинаров + анализ
- **Видео** — извлечение аудио и транскрипция

## 🎯 Возможности

### PDF обработка
- Извлечение текста из PDF любого размера (включая таблицы)
- Умный чанкинг с учетом семантических границ
- 5 режимов анализа: саммари, концепции, инструменты, вопросы, полный

### Аудио транскрипция
- Поддержка OpenAI Whisper API, локального Whisper, AssemblyAI
- Автоматическая разбивка длинных записей
- Извлечение ключевых идей, инструментов, цитат
- Таймкоды для навигации

## 🚀 Быстрый старт

### 1. Установка

```bash
pip install -r requirements.txt

# Для аудио также нужен ffmpeg:
# Ubuntu: sudo apt install ffmpeg
# Mac: brew install ffmpeg
```

### 2. API ключи

```bash
# Для Claude (обязательно)
export ANTHROPIC_API_KEY="sk-ant-..."

# Для транскрипции (OpenAI Whisper - рекомендуется)
export OPENAI_API_KEY="sk-..."
```

### 3. Использование

```bash
# Универсальный обработчик (автоопределение типа)
python process.py материал.pdf
python process.py семинар.mp3 --title "Позитивная психология"
python process.py папка/ --batch

# PDF отдельно
python pdf_ai_processor.py process книга.pdf --mode full_analysis

# Аудио отдельно
python transcribe.py семинар.mp3 --title "Коучинг" --speaker "Улановский"

# Интерактивный режим (вопросы по базе знаний)
python interactive.py
```

## 📁 Структура проекта

```
materials_processor/
├── process.py              # 🎯 Универсальный обработчик
├── pdf_ai_processor.py     # PDF обработка
├── audio_transcriber.py    # Аудио транскрипция
├── transcribe.py           # Быстрая транскрипция
├── interactive.py          # Q&A по базе знаний
├── batch_processor.py      # Массовая обработка
├── config.json             # Настройки
├── requirements.txt        # Зависимости
│
├── output/                 # Результаты обработки
│   ├── *_processed.json    # JSON с результатами
│   ├── *_processed.md      # Markdown для чтения
│   └── *.md                # Саммари семинаров
│
├── transcripts/            # Чистые транскрипции
│   └── *_transcript.txt
│
└── knowledge_base/         # База знаний
    └── knowledge_base.json
```

## 🎙️ Транскрипция аудио

### Провайдеры

| Провайдер | Качество | Скорость | Стоимость | Русский |
|-----------|----------|----------|-----------|---------|
| OpenAI Whisper | ⭐⭐⭐⭐⭐ | Быстро | $0.006/мин | ✅ Отлично |
| Local Whisper | ⭐⭐⭐⭐ | Зависит от GPU | Бесплатно | ✅ Хорошо |
| AssemblyAI | ⭐⭐⭐⭐ | Быстро | $0.01/мин | ✅ Хорошо |

### Примеры

```bash
# OpenAI Whisper (рекомендуется)
python transcribe.py семинар.mp3

# С метаданными
python transcribe.py лекция.m4a \
  --title "Позитивная психология в коучинге" \
  --speaker "Алексей Улановский" \
  --date "2025-11-25" \
  --module "2"

# Только транскрипция без анализа
python transcribe.py запись.mp3 --transcript-only

# Локальный Whisper (бесплатно)
python transcribe.py семинар.mp3 --provider local_whisper
```

### Поддерживаемые форматы

- **Аудио**: mp3, m4a, wav, ogg, flac, aac
- **Видео**: mp4, webm, mkv, avi, mov

## 📊 Режимы обработки

| Режим | PDF | Аудио | Описание |
|-------|-----|-------|----------|
| `summary` | ✅ | — | Структурированное саммари |
| `key_concepts` | ✅ | — | Концепции и модели (JSON) |
| `coaching_tools` | ✅ | — | Инструменты коучинга |
| `questions` | ✅ | — | Вопросы для рефлексии |
| `full_analysis` | ✅ | ✅ | Всё вместе |

## 💡 Сценарии использования

### После модуля СКОЛКОВО

```bash
# 1. Транскрибируем записи семинаров
python transcribe.py "День1_Улановский.m4a" --title "Позитивная психология" --speaker "Улановский" --module "2"
python transcribe.py "День2_Клековкина.m4a" --title "Этика коуча" --speaker "Клековкина" --module "2"

# 2. Обрабатываем PDF материалы
python pdf_ai_processor.py process "Раздатка_Модуль2.pdf"

# 3. Строим единую базу знаний
python pdf_ai_processor.py build-kb

# 4. Работаем с материалами
python interactive.py
> Что такое ABCDE модель?
> Какие 7 областей коучинга позитивной психологии?
```

### Подготовка к модулю

```bash
# Обрабатываем предчтение
python process.py предчтение/ --batch

# Готовим вопросы
python pdf_ai_processor.py process материал.pdf --mode questions
```

## 💰 Оценка стоимости

### Транскрипция (OpenAI Whisper)
- 1 час аудио ≈ $0.36
- Семинар 3 часа ≈ $1.08

### Анализ (Claude Sonnet)
- 1 час транскрипции ≈ 50K токенов
- Анализ ≈ $0.30-0.50

**Итого за 3-часовой семинар: ~$1.50-2.00**

## 🔧 Настройки

Файл `config.json`:

```json
{
    "model": "claude-sonnet-4-20250514",
    "max_tokens_per_chunk": 4000,
    "transcription_provider": "openai",
    "language": "ru"
}
```

## 🐛 Решение проблем

### "ffmpeg not found"
```bash
# Ubuntu
sudo apt install ffmpeg

# Mac
brew install ffmpeg

# Windows
# Скачать с https://ffmpeg.org/
```

### "Rate limit exceeded"
- Уменьшите размер чанков в config.json
- Добавьте паузы между запросами

### Плохое качество транскрипции
- Используйте OpenAI Whisper вместо локального
- Проверьте качество исходной записи
- Укажите язык явно: `--language ru`

## 📞 Поддержка

Этот инструмент создан для программы SKOLKOVO Executive Coaching & Mentoring.

При возникновении вопросов обращайтесь к своему AI-коучу в чате Claude.

---

**Версия:** 2.0  
**Дата:** 2025-11-25  
**Автор:** Claude AI Assistant
